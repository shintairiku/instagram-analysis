# Backend environment variables (example)
#
# Copy to `backend/.env` (or export in your environment) and fill in real values.
#
# Required (Supabase backend SDK)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional (used only if you later add client-side Supabase access)
SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional (only needed for Supabase CLI / direct SQL tooling)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/instagram_analysis

# Optional (Facebook/Instagram Graph API App credentials; used by config/credential checks)
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret

# Optional (used by GitHub Actions scripts for Slack notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
