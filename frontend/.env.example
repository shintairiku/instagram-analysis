# Frontend environment variables (example)
#
# Copy to `frontend/.env.local` and fill in real values.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-only (used by Next.js route handlers; NOT exposed to browser)
# Must match backend's COLLECTION_TRIGGER_TOKEN
COLLECTION_TRIGGER_TOKEN=your_long_random_secret
